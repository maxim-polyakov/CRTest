version: '3.8'
services:
  client:
    container_name: crclient
    build:
      dockerfile: Dockerfile
      context: .
    env_file:
      - .env
    ports:
      - "3016:3016"
    restart: unless-stopped
    networks:
      - crc-network
networks:
  crc-network:
    driver: bridge