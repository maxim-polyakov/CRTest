version: '3.8'
services:
  client:
    container_name: crclient
    build:
      dockerfile: Dockerfile
      context: .
    env_file:
      - .env
    ports:
      - "3001:3001"
    restart: unless-stopped